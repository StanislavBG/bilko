{
  "version": "1.1.0",
  "workflows": [
    {
      "id": "echo-test",
      "name": "Bilko Echo Test",
      "mode": "n8n",
      "webhookPath": "bilko-echo-test",
      "description": "Verifies end-to-end connectivity between Bilko and n8n. Sends a test payload to the n8n webhook and confirms the round-trip response is received correctly. Use this to validate that the n8n integration is properly configured and responding.",
      "instructions": "Click 'Execute' to send a test message to n8n. The workflow will echo back your message with a timestamp, confirming the connection is working.",
      "category": "system"
    },
    {
      "id": "rules-audit",
      "name": "Rules Audit",
      "mode": "local",
      "description": "Analyzes the rule framework for structural integrity, cross-references, and coverage gaps. Validates that all rules are properly formatted, have valid dependencies, and follow the partitioning scheme defined in ARCH-006.",
      "instructions": "This workflow is executed via agent reasoning. Ask the agent: 'Run a rule audit' or 'Act as Rule Architect'. The agent will analyze all rules following the AGT-002-RULES protocol (agents/002-rules-audit.md) and provide a comprehensive audit report.",
      "handler": "rulesAudit",
      "category": "audit"
    },
    {
      "id": "code-audit",
      "name": "Code Audit",
      "mode": "local",
      "description": "Validates that the codebase correctly implements the rules defined in /rules/. Checks for rule violations, missing implementations, and alignment between code behavior and rule specifications.",
      "instructions": "This workflow is executed via agent reasoning. Ask the agent: 'Run a code audit' or specify which rules to audit. The agent will analyze the code following the AGT-002-CODE protocol (agents/002-code-audit.md) and report any violations or gaps.",
      "handler": "codeAudit",
      "category": "audit"
    },
    {
      "id": "european-football-daily",
      "name": "[PROD] European Football Daily",
      "mode": "n8n",
      "webhookPath": "european-football-daily",
      "description": "PRODUCTION workflow. Daily automated European football news aggregation. Runs at 00:00 to search the last 24 hours of European football news, identifies the most positive/cheerful event using sentiment analysis, generates an AI image, and publishes a congratulatory post with AI transparency disclosure.",
      "instructions": "This is the PRODUCTION workflow. It runs automatically at 00:00 daily via n8n Schedule Trigger. Uses fixed callback URL to bilkobibitkov.replit.app. For testing, use the DEV workflow instead.",
      "category": "content"
    },
    {
      "id": "dev-european-football-daily",
      "name": "[DEV] European Football Daily",
      "mode": "n8n",
      "webhookPath": "dev-european-football-daily",
      "description": "DEVELOPMENT workflow. Same pipeline as PROD but with dynamic callback URLs and manual trigger only. Use this for testing new features before promoting to PROD.",
      "instructions": "Manual execution only. Uses dynamic callback URLs so it works with any Replit dev domain. Both DEV and PROD can be active simultaneously (different webhook paths).",
      "category": "content"
    }
  ]
}
