/**
 * Bilko Flow API — Flow Mutations
 *
 * Re-exports from bilko-flow/react. The mutation engine is maintained
 * in the bilko-flow package and consumed here for backward compatibility.
 */
export { applyMutation, createBlankStep, generateStepId } from "bilko-flow/react";
export type { FlowMutation, MutationResult, MutationValidationError } from "bilko-flow/react";
